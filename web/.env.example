# Base URL of the backend API (jaygalagbe/backend), used for server-side data fetching.
# The Next.js dev server also defaults to port 3000, so run the backend with
# PORT=4000 (or update this to match wherever it's actually listening).
API_URL="http://localhost:4000"
